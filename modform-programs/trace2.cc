#include <iostream>

#include "slint.h"
#include "characters.h"

int main(int argc, char ** argv) {
    int level = atoi(argv[1]);
    int chi = atoi(argv[2]);
    int ntraces = atoi(argv[3]);
    return 0;
}
